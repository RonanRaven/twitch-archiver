version: '2.1'
services:
  twitch-archiver:
    image: ronanraven/twitch-archiver:latest
    container_name: twitch-archiver
    volumes:
      - ./vods:/vods
    environment:
      - STR_APPID=
      - STR_APPSECRET=
      - STR_CHANNEL=
    restart: unless-stopped
